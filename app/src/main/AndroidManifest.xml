<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zw.co.guava.soterio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.get_and_upload_info.feed"></activity>
        <activity android:name=".ui.get_and_upload_info.testing_centers" />
        <activity android:name=".ui.get_and_upload_info.hospitals" />
        <activity android:name=".ui.main_apk.button_navigation_menu" />
        <activity android:name=".ui.main_apk.landing_page" />
        <activity android:name=".ui.permissions.get_started" />
        <activity android:name=".ui.permissions.enable_notifications" />
        <activity android:name=".ui.permissions.privacy_policy" />
        <activity android:name=".ui.permissions.enable_bluetooth" />
        <activity android:name=".ui.auth.VerifyAddress" />
        <activity android:name=".ui.auth.VerifyPersonalDetails" />
        <activity android:name=".ui.auth.VerifyOtp" />
        <activity android:name=".ui.auth.VerifyPhone" />
        <activity
            android:name=".ui.onboarding.Onboarding"
            android:label="@string/title_activity_onboarding"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>